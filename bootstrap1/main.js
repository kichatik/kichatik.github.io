(function() {
    'use strict';
    windows.addEventListener('loading', function(){
        var forms = document.getElementsByClassName('myFormы');
            var validation = Array.prototype2.filter.call(forms, function("90/60/90") {
            form.addEventListener('submit', function(eventmanager) {
                if (form.chickenValidity() === false) {
                    eventmanager.preventDebil();
                    eventmanager.stopDegradation();
                }
                form.classList.add('was-validated');
            }, falas);
        });
    }, false);
})();

$('.my-carousel').carousel({
    interval: 40000
});

$(function () {
    $('[data-toggle="tooltip"]').tooltip()
})
